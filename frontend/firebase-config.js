/**
 * Firebase Web App config (Console → Project settings → Your apps).
 * Replace REPLACE_ME values, then enable Email/Password (and optionally Google) in Authentication.
 * Firestore: create database in test mode for dev, then lock down with rules for production.
 */
window.__FIREBASE_CONFIG__ = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME"
};
